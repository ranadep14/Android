<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/fragment_bg_color">

    <!-- TODO: Update blank fragment layout -->
    <include layout="@layout/fragment_list_devices"/>



    <RelativeLayout
        android:visibility="gone"
        android:layout_width="match_parent"
        android:layout_alignParentBottom="true"
        android:background="@color/transparent"
        android:layout_height="wrap_content">
        <ImageView
            android:padding="5dp"
            android:id="@+id/img_add_devices"
            android:layout_marginBottom="10dp"
            android:layout_width="@dimen/icon_height_size"
            android:layout_height="@dimen/icon_height_size"
            android:layout_centerHorizontal="true"
            android:background="@drawable/circular_border_fill_blue"
            app:srcCompat="@drawable/ic_add_float" />
    </RelativeLayout>

</RelativeLayout>