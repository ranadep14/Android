<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/rel_main"
    android:background="@color/installation_trans_color"
    tools:context="com.cloudsinc.welltekmobile.native_v2_welltek.activities.Demo_scene_popup">
    <LinearLayout
        android:layout_centerInParent="true"
        android:layout_width="500dp"
        android:orientation="vertical"
        android:layout_height="300dp"
        tools:ignore="UselessParent">

        <RelativeLayout
            xmlns:android="http://schemas.android.com/apk/res/android"
            android:id="@+id/rel_loading"
            android:layout_gravity="center_horizontal"
            android:layout_width="120dp"
            android:background="@color/transparent"
            android:layout_centerInParent="true"
            android:layout_marginBottom="-50dp"
            android:layout_height="120dp">

            <ProgressBar
                android:layout_centerInParent="true"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:indeterminateDrawable="@drawable/loading_circle_shape"
                android:indeterminate="true"
                />
            <ImageView
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:padding="25dp"
                android:src="@drawable/app_logo_transperant"/>
        </RelativeLayout>
        <TextView
            android:layout_gravity="center_vertical"
            android:id="@+id/txt_dialog_title"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_margin="20dp"
            android:padding="10dp"
            android:text="Currently your demo scene is active. \n Please wait."
            android:textColor="@color/white"
            android:gravity="center"
            android:textSize="@dimen/heading_text_size"
            />
    </LinearLayout>
</RelativeLayout>

